import 'package:flutter/material.dart';

class AllActiveTodoList extends StatelessWidget {
  const AllActiveTodoList({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Active Todo List"),
      ),
    );
  }
}
